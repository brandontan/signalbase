{
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "python3 -m uvicorn api:app --host 0.0.0.0 --port $PORT --proxy-headers --forwarded-allow-ips=*"
  },
  "volumes": {
    "/app/data": "signalbase_data"
  }
}
